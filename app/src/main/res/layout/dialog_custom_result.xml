<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="24dp"
    app:cardCornerRadius="28dp"
    app:cardElevation="12dp"
    app:cardBackgroundColor="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingTop="32dp"
        android:paddingBottom="24dp"
        android:paddingHorizontal="24dp"
        android:gravity="center">

        <!-- Biểu tượng hoành tráng -->
        <TextView
            android:id="@+id/tvDialogIcon"
            android:layout_width="86dp"
            android:layout_height="86dp"
            android:text="🏆"
            android:textSize="44sp"
            android:gravity="center"
            android:background="@drawable/bg_button_quiz"
            android:backgroundTint="#FFF3E0"
            android:layout_marginBottom="20dp"/>

        <!-- Tiêu đề Dialog -->
        <TextView
            android:id="@+id/tvDialogTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Vượt Ải Thành Công!"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="#E65100"
            android:gravity="center"
            android:layout_marginBottom="12dp"/>

        <!-- Lời nhắn -->
        <TextView
            android:id="@+id/tvDialogMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tuyệt vời! Bạn nhận được 50 XP.\nBạn có muốn chiến tiếp ải sau?"
            android:textSize="16sp"
            android:textColor="#546E7A"
            android:gravity="center"
            android:lineSpacingExtra="4dp"
            android:layout_marginBottom="32dp"/>

        <!-- Nút Chính (Chiến tiếp / Tuyệt vời) -->
        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnDialogPositive"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:background="@drawable/bg_button_quiz"
            android:backgroundTint="#4CAF50"
            android:text="CHIẾN TIẾP 🚀"
            android:textColor="#FFFFFF"
            android:textSize="17sp"
            android:textStyle="bold"
            android:elevation="4dp"
            android:layout_marginBottom="12dp"/>

        <!-- Nút Phụ (Về bản đồ) -->
        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnDialogNegative"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:background="@drawable/bg_button_quiz"
            android:backgroundTint="#F5F7FA"
            android:text="VỀ BẢN ĐỒ"
            android:textColor="#757575"
            android:textSize="17sp"
            android:textStyle="bold"
            android:elevation="0dp"/>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>